{
  "name": "openclaw-trmnl",
  "version": "0.0.0",
  "private": true,
  "description": "TRMNL e-ink display toolkit for OpenClaw",
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "typecheck": "pnpm -r typecheck",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "changeset publish"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@9.0.0",
  "devDependencies": {
    "@changesets/cli": "^2.30.0"
  }
}
